
pipeline {
    agent any
stages {

        stage('Build') {
            steps {
             sh 'mvn -v'
            }
        }
        stage('Test') {
            steps {
            sh 'mvn test'
        }
         post{
              always{
            junit 'target/surefire-report/*.xml'
       }

    }
}
}
}
